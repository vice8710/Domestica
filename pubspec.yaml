name: domestica
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0        # Specify actual latest version here
  firebase_auth: ^5.3.1     # Specify actual latest version here
  google_sign_in: ^6.1.0       # Specify actual latest version here
  flutter_facebook_auth: ^7.1.1 # Specify actual latest version here
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/kitchen.jpg
    - assets/window.jpg
    - assets/livingroom.jpg
    - assets/acservice.jpg
